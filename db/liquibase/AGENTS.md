# DB開発ルール

- `context`は必ず指定する。
- `context`に`release`を追加した場合、以降編集不可。`release`入りしていないものはまだ変更される可能性があることを考慮する。

# 注意事項

- `changes/*.sql`の注意
  - DDL と DML の混在不可: 1つのトランザクション内でテーブル定義変更（DDL）とデータ操作（DML）は同時実行不可。また、1つのトランザクションに含められる DDL は1文のみ。COMMENTすらsqlファイルを分ける必要がある。そのため`*.sql`ファイルが大量にできる予定だが、仕様通り。
